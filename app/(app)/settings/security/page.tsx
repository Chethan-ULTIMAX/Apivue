export default function SecurityPage() {
	return <main className="page-shell"><p className="eyebrow">Security</p><h1>Protected data needs protected boundaries.</h1><p className="lede">OAuth connections and credential storage will be implemented with a secure provider in a later phase.</p></main>;
}
