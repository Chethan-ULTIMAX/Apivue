import Link from "next/link";
export default function HistoryPage(){return <main className="page-shell"><p className="eyebrow">History / snapshots</p><h1>History starts with explicit consent.</h1><p className="lede">Phase 2 defines normalized snapshots for public metrics, but does not persist profiles in a browser or database. That prevents accidental retention of data while account connections are not available.</p><section className="detail-grid"><div><article className="detail-row"><strong>Snapshot model</strong><span>Platform, public subject identifier, capture timestamp, and normalized non-sensitive metrics.</span></article><article className="detail-row"><strong>Self comparison</strong><span>Available when snapshots are explicitly stored in a future authenticated workspace.</span></article></div><aside className="notice"><p className="section-label">Current state</p><p>No snapshots stored.</p><Link className="button button-primary" href="/profiles">Analyze a public profile</Link></aside></section></main>}
