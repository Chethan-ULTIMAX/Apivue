"use client";
import Link from "next/link"; import { useEffect, useState } from "react";
const commands=[['Explore platforms','/explore'],['API Playground','/playground'],['Compare profiles','/compare'],['Profiles','/profiles'],['Dashboard','/dashboard'],['GitHub analytics','/platforms/github'],['Codeforces analytics','/platforms/codeforces']];
export function CommandPalette(){const[open,setOpen]=useState(false),[query,setQuery]=useState("");useEffect(()=>{const listener=(e:KeyboardEvent)=>{if((e.key==='k'&&(e.ctrlKey||e.metaKey))|| (e.key==='/' && !(e.target instanceof HTMLInputElement))){e.preventDefault();setOpen(true)}if(e.key==='Escape')setOpen(false)};window.addEventListener('keydown',listener);return()=>window.removeEventListener('keydown',listener)},[]);const items=commands.filter(([label])=>label.toLowerCase().includes(query.toLowerCase()));return <><button className="search-trigger" onClick={()=>setOpen(true)} aria-label="Open command palette">Search <kbd>⌘K</kbd></button>{open?<div className="command-backdrop" role="dialog" aria-modal="true" aria-label="Command palette" onMouseDown={()=>setOpen(false)}><div className="command-palette" onMouseDown={e=>e.stopPropagation()}><input autoFocus value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search navigation…" aria-label="Search commands"/>{items.map(([label,href])=><Link key={href} href={href} onClick={()=>setOpen(false)}>{label}<span>↗</span></Link>)}{!items.length?<p className="muted">No navigation match.</p>:null}</div></div>:null}</>}
