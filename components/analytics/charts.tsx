"use client";
export function StatCard({label,value}:{label:string;value:string|number}) { return <article className="stat-card"><span>{label}</span><strong>{typeof value === "number" ? value.toLocaleString() : value}</strong></article>; }
export function BarChart({title,data}:{title:string;data:[string,number][]}) { const max=Math.max(1,...data.map(([,value])=>value)); return <section className="chart"><h3>{title}</h3>{data.length ? <div className="bars">{data.map(([label,value])=><div className="bar-row" key={label}><span>{label}</span><i><b style={{width:`${(value/max)*100}%`}}/></i><em>{value}</em></div>)}</div>:<p className="muted">No source data is available for this chart.</p>}</section>; }
export function LineChart({title,data}:{title:string;data:{timestamp:string;value:number}[]}) { if(!data.length)return <section className="chart"><h3>{title}</h3><p className="muted">No timeline returned by the source.</p></section>; const values=data.map(p=>p.value), min=Math.min(...values), max=Math.max(...values), range=max-min||1; const points=data.map((p,i)=>`${(i/(data.length-1||1))*100},${100-((p.value-min)/range)*90-5}`).join(" "); return <section className="chart"><h3>{title}</h3><svg viewBox="0 0 100 100" preserveAspectRatio="none" role="img" aria-label={title}><polyline points={points} fill="none" stroke="currentColor" strokeWidth="2" vectorEffect="non-scaling-stroke"/></svg><div className="chart-range"><span>{data[0]?.value}</span><span>{data.at(-1)?.value}</span></div></section>; }
