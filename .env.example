 # Add server-only provider credentials here when authentication is implemented.
 # Never expose these values to client components or commit a real .env file.
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=
GITHUB_ID=
GITHUB_SECRET=
